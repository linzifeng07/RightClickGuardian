# 参与贡献

欢迎提交 Issue 和 Pull Request。

提交代码前请：

1. 保持兼容 .NET Framework 4.8 和 C# 5。
2. 不要提交本机策略、注册表导出、测试数据或编译产物。
3. 运行 `.\build.ps1 -RunTests`。
4. 对注册表、文件移动和任务计划相关改动补充恢复路径。
5. UI 改动需兼顾高 DPI、中文文本和大量菜单项目时的流畅度。

Pull Request 请说明改动目的、用户影响、验证方式和可能的系统风险。
